def build(bld):

    bld.DefineGem(
        # Add custom build options here
        includes    = [Path('Code/CryEngine/CryAction'),
					   Path('Code/SDKs/CoherentGT/include')],
		
		win_features = [ 'CoherentGT' ]
    )
